/* eslint-env jest */
import noopAction from '../../../../testUtils/noopAction';

export const runRegistrationTypeChangeValidationsForPrimaryAndGuest = jest.fn(noopAction);
export const setRegistrationTypeId = jest.fn(noopAction);
